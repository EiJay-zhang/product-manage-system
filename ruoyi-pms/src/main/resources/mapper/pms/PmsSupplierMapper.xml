<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.pms.mapper.PmsSupplierMapper">
    <resultMap id="PmsSupplierResult" type="PmsSupplier">
        <id property="supplierId" column="supplier_id"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="contactName" column="contact_name"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <sql id="selectVo">
        select supplier_id, supplier_name, contact_name, phone, status, del_flag, create_by, create_time, update_by, update_time, remark
        from pms_supplier
    </sql>
    <select id="selectSupplierList" parameterType="PmsSupplier" resultMap="PmsSupplierResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="supplierName != null and supplierName != ''">AND supplier_name like concat('%', #{supplierName}, '%')</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        order by supplier_id desc
    </select>
    <select id="selectSupplierById" resultMap="PmsSupplierResult">
        <include refid="selectVo"/> where supplier_id = #{supplierId}
    </select>
    <select id="checkSupplierNameUnique" resultMap="PmsSupplierResult">
        <include refid="selectVo"/> where supplier_name = #{supplierName} and del_flag = '0' limit 1
    </select>
    <select id="countProductBySupplierId" resultType="int">
        select count(1) from pms_product where supplier_id = #{supplierId} and del_flag = '0'
    </select>
    <select id="countPurchaseBySupplierId" resultType="int">
        select count(1) from pms_purchase where supplier_id = #{supplierId}
    </select>
    <insert id="insertSupplier" parameterType="PmsSupplier" useGeneratedKeys="true" keyProperty="supplierId">
        insert into pms_supplier (supplier_name, contact_name, phone, status, del_flag, create_by, create_time, remark)
        values (#{supplierName}, #{contactName}, #{phone}, #{status}, '0', #{createBy}, sysdate(), #{remark})
    </insert>
    <update id="updateSupplier" parameterType="PmsSupplier">
        update pms_supplier
        <set>
            <if test="supplierName != null">supplier_name = #{supplierName},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_by = #{updateBy}, update_time = sysdate()
        </set>
        where supplier_id = #{supplierId}
    </update>
    <update id="deleteSupplierByIds">
        update pms_supplier set del_flag = '2' where supplier_id in
        <foreach collection="array" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>
</mapper>
