<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.pms.mapper.PmsStockLogMapper">
    <resultMap id="PmsStockLogResult" type="PmsStockLog">
        <id property="logId" column="log_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="changeType" column="change_type"/>
        <result property="beforeQty" column="before_qty"/>
        <result property="changeQty" column="change_qty"/>
        <result property="afterQty" column="after_qty"/>
        <result property="bizType" column="biz_type"/>
        <result property="bizId" column="biz_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <select id="selectStockLogList" resultMap="PmsStockLogResult">
        select log_id, product_id, product_name, change_type, before_qty, change_qty, after_qty, biz_type, biz_id, create_by, create_time, remark
        from pms_stock_log
        <where>
            <if test="productId != null">AND product_id = #{productId}</if>
            <if test="productName != null and productName != ''">AND product_name like concat('%', #{productName}, '%')</if>
            <if test="changeType != null and changeType != ''">AND change_type = #{changeType}</if>
            <if test="beginTime != null">AND create_time &gt;= #{beginTime}</if>
            <if test="endTime != null">AND create_time &lt;= #{endTime}</if>
            <if test="params.beginTime != null and params.beginTime != ''">AND create_time &gt;= #{params.beginTime}</if>
            <if test="params.endTime != null and params.endTime != ''">AND create_time &lt;= #{params.endTime}</if>
        </where>
        order by log_id desc
    </select>
    <insert id="insertStockLog" useGeneratedKeys="true" keyProperty="logId">
        insert into pms_stock_log (product_id, product_name, change_type, before_qty, change_qty, after_qty, biz_type, biz_id, create_by, create_time, remark)
        values (#{productId}, #{productName}, #{changeType}, #{beforeQty}, #{changeQty}, #{afterQty}, #{bizType}, #{bizId}, #{createBy}, sysdate(), #{remark})
    </insert>
</mapper>
